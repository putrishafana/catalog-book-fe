Front End

- Dashboard dengan navigasi ke Authors, Books, Publishers
- CRUD Authors, Books, Publishers
- Pagination & search
- Login/logout menggunakan JWT
- Form validasi untuk semua input

1. Clone repo
2. Copy `env.local` ke `.env.local` dan sesuaikan konfigurasi
3. Setting URL di .env.local
4. npm install
5. npm run dev
